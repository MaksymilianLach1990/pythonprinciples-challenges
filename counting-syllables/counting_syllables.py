
def countingSyllables(word:str):
    """The function takes 1 argument, type string.
        The function counts how many syllables are
        in the given value.

        The  function returns the number of syllables. 
        """
    return len(word.split('-'))
    
